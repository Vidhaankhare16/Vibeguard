#!/usr/bin/env node

// ============================================================
// VibeSec CLI — Entry point
// ============================================================

import { createCLI } from '../src/cli.js';

createCLI(process.argv);
